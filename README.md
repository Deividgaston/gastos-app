# Gastos 2N — App (GH Pages)

Sube todo este proyecto a GitHub Pages. Configura Firebase con Google y dominios autorizados (deividgaston.github.io, localhost, 127.0.0.1).


## Configuración
Edita `config.js` con tu `firebaseConfig` exacto desde Firebase → Project settings → SDK snippet.
